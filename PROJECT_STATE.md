# L Cafe project state

Updated: 2026-09-19 (Asia/Tehran)

This is the authoritative mutable state ledger. Stable architecture and
procedures remain in `HANDOFF.md` and `OPERATIONS.md`; do not duplicate changing
SHAs or migration status elsewhere.

| Field | Authoritative value | Provenance |
| --- | --- | --- |
| Source baseline | The commit containing this ledger on `main` / `origin/main`; accepted pre-P1-B documentation baseline was `a0db4d7fb946c512ec04ff69c6d59ca15eba4e79` | commit-relative authoritative record |
| Source transition | Liara/VPS cutover is accepted on release `b8afd23722e6d8400af1207b0474de1f1888df83`. The former ParsPack production release `e37e18ba7f99200cde6f69e61cffe3b221309a03` is retained unchanged only as rollback material. | directly verified cutover result plus retained rollback record |
| Production SHA | Main Site production now runs on the Liara VPS from release `b8afd23722e6d8400af1207b0474de1f1888df83` at `/srv/lcafe-site/current`. The retired-but-retained ParsPack runtime remains at `e37e18ba7f99200cde6f69e61cffe3b221309a03` as rollback material and is not deleted. | live VPS release symlink plus directly verified public HTTPS production state |
| Release state | Active production release is `b8afd23722e6d8400af1207b0474de1f1888df83` at `/srv/lcafe-site/current`. ParsPack release `e37e18ba7f99200cde6f69e61cffe3b221309a03` is no longer the edit authority and remains available only for rollback. | live VPS release symlink plus cutover verification |
| Migration state | `001_menu_admin`, `002_admin_roles`, `003_media_lifecycle`, and `004_admin_session_epoch` are active. Migration `004` completed through the release Provisioning runner; its marker and exact `BIGINT UNSIGNED NOT NULL DEFAULT 1 AFTER role` schema were verified before Auth activation. | directly verified production migration result |
| Role state | `admin` remains active `owner`; `cashier` remains active `cashier`. Both passwords were rotated on 2026-09-05, lockout state was reset, and both new logins were verified. | directly verified production rotation and login results |
| Deployment state | Liara/VPS production cutover is complete and accepted. `/`, `/menu`, `/admin/`, `/api/session`, managed menu/media, HTTPS redirects, and the Node API readiness path were verified. The VPS Admin is the sole edit authority. ParsPack remains intact and unchanged for rollback only. | directly verified production cutover result |
| Public menu snapshot | Live production serves revision 65 with `current.json` SHA-256 `3faeebfaa7c786c0d9e5c1ddfd5fd233f2550726c64028d741b9cec74f4fe3d7`; `previous.json` is revision 64 with SHA-256 `dd35c31b4af97313a324dbab90bee03ee53f49345ce8ceb3951d6248ec2d0f0e`. The final ParsPack reconciliation was closed by explicit owner attestation on 2026-09-18 that no menu edits were published on ParsPack after the 2026-09-17 20:45 export; no capture newer than that export exists, and the byte-identical Downloads set was re-verified against these hashes. This is an owner attestation, not a fresh technical capture. | owner attestation plus re-verified capture hashes and live public snapshot |
| Architecture | Current production is React/Vite static Landing, Menu, and Admin behind Nginx on the Liara VPS, with one isolated Node 24/Fastify/SQLite control plane on `127.0.0.1:3100`. Public menu/media remain static snapshot assets. L Cafe Operations remains isolated on its existing service/port. ParsPack PHP/MySQL is retained only as rollback. | live VPS architecture plus verified isolation |
| VPS migration preparation | Production cutover to the shared Liara VPS is complete and accepted on release `b8afd23722e6d8400af1207b0474de1f1888df83`. Public DNS resolves `l-cafe.ir` A to `92.114.51.128` and `www.l-cafe.ir` CNAME to the apex on Liara nameservers, confirmed authoritatively and on three public resolvers. The Let’s Encrypt certificate `l-cafe.ir` covers `l-cafe.ir` and `www.l-cafe.ir`, is valid to 2026-12-17, and was issued through the `/var/www/letsencrypt` webroot without stopping Nginx; the renewal dry-run passed. The Main Site production Nginx vhost is active as a separate site and the temporary HTTP bootstrap is disabled. A real owner Admin login and logout over HTTPS passed with no content change. Operations remained active on `127.0.0.1:3000` with its Nginx file untouched. | directly verified public production state |
| Rollback state | DNS-level rollback destination is the pre-cutover ParsPack shared IP `45.139.11.60`, supplied by the owner from ParsPack cPanel on 2026-09-18. ParsPack/PHP/MySQL and the prior release `e37e18ba7f99200cde6f69e61cffe3b221309a03` remain intact, unchanged, and not retired. The VPS-side rollback set is the verified backup generation `20260918T121234Z` (SQLite `ce432c208c82c41375c466de81d0ec2220f9ab2251428f2bbed4ff52ed4cdf25`, content archive `4599925d6570ff368a877278abf4efcaa7da3b24ce4ec6365c96ef244b9bfa47`), restore-drilled and copied off-host. | owner-supplied cPanel record plus directly verified backup and restore results |
| Runtime ownership | VPS SQLite, private config, sessions, revisions, originals, managed snapshots/media, backups, and Nginx/systemd runtime configuration are persistent host-owned state outside immutable releases. The retained ParsPack runtime is rollback-only and must remain untouched unless rollback is explicitly authorized. | live VPS ownership model plus retained rollback contract |
| Repository governance | `Lcafee/su` is public; normal direct pushes to `main` remain allowed; every push runs the frontend-only GitHub Pages preview workflow; Pages is pre-production only and does not approve, generate, or deploy a production release; `backup-remote` is unchanged | GitHub-observed plus source-defined workflow |
| Preview state | GitHub Pages uses the project base `/su/`; the Pages artifact is restricted to Landing, `/menu/`, `/menu2/`, required public assets/fonts, and preview copies of the tracked development menu fixture; the complete preview is `noindex,nofollow` | source-defined workflow and build guard |
| Blockers | No blocking item remains for Main Site production operation. The final ParsPack reconciliation rests on owner attestation rather than a fresh technical capture. Destructive historical archive/media cleanup remains a separate deferred concern. | current migration execution state |
| Current phase | Liara is live production for `l-cafe.ir` on release `b8afd23722e6d8400af1207b0474de1f1888df83` over HTTPS at revision 65/65. ParsPack/PHP/MySQL is retained unchanged as rollback and is not retired. PR #7 is not merged. During residual DNS cache expiry the VPS Admin is the only permitted edit authority. | directly verified live production state |
| Next action | Use the VPS Admin as the sole edit authority. For code changes use the manual production workflow: commit/push the intended source, build/package the exact release, take a production backup, deploy only the required release, then verify `/`, `/menu`, `/admin/`, and `/api/session`. Keep ParsPack untouched as rollback; PR #7 merge and ParsPack retirement remain separate explicit decisions. | accepted post-cutover operating plan |
