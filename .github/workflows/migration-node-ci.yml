name: Main Site Node migration CI

on:
  push:
    branches:
      - migration/node-sqlite-vps
  pull_request:
    paths:
      - 'server-node/**'
      - 'deploy/vps/**'
      - '.github/workflows/migration-node-ci.yml'
      - 'src/**'
      - 'admin/**'
      - 'assets/**'
      - '*.html'
      - 'package.json'
      - 'package-lock.json'
      - 'vite*.js'

permissions:
  contents: read

jobs:
  node-api:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
          cache-dependency-path: server-node/package-lock.json
      - name: Install locked API dependencies
        working-directory: server-node
        run: npm ci --no-audit --no-fund
      - name: Run API contract tests
        working-directory: server-node
        run: npm test
      - name: Check migration script syntax
        working-directory: server-node
        run: |
          node --check scripts/migrate.mjs
          node --check scripts/import-parspack.mjs
          node --check scripts/verify-import.mjs
          node --check scripts/backup-state.mjs
          node --check src/server.mjs
          node --check ../deploy/vps/package-staging-release.mjs
          node --check ../deploy/vps/verify-staging-release.mjs

  frontend-regression-build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
      - name: Install existing frontend dependencies
        run: npm ci --no-audit --no-fund
      - name: Build unchanged frontend
        run: npm run build
      - name: Validate dist contract
        run: npm run validate:dist
