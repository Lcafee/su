# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

مخاطب اصلی: **مشتری نشسته پشت میز در کافه**، روی موبایل شخصی.

**مسیر اصلی — تأییدشده:**

    اسکن QR ← صفحه‌ی لندینگ ← زدن «مشاهده منو» ← صفحه‌ی منو

لندینگ نقطه‌ی ورود اجباری است، نه صفحه‌ی فرعی. این یعنی لندینگ برای کسی
طراحی می‌شود که **از قبل داخل کافه نشسته** — نه برای غریبه‌ای که باید متقاعد
شود سر بزند. کار لندینگ یک لحظه‌ی کوتاه معرفی برند است و بعد تحویل سریع به منو.

نتایج مستقیم این واقعیت:

- هر چیزی که میان اسکن و منو بایستد، هزینه دارد و باید توجیه داشته باشد.
- موبایل حالت پیش‌فرض است، نه یک بریک‌پوینت فرعی.
- شرایط شبکه، دیتای همراه داخل ساختمان است — نه وای‌فای دفتر.
- کاربر در حال تصمیم‌گیری بین آیتم‌هاست؛ «پیدا کردن سریع قیمت» و
  «مقایسه‌پذیری» بر تأثیر بصری اولویت دارد.

**وضعیت اندازه‌گیری‌شده‌ی این مسیر (۳۷۵×۸۱۲):** دکمه‌ی «مشاهده منو» در
ارتفاع ۱۹۳۳ پیکسل قرار دارد — یعنی ۲٫۳۸ ویوپورت پایین‌تر از ابتدای صفحه.
مشتری باید حدود ۱۴۴۶ پیکسل اسکرول کند تا تنها راه رسیدن به منو ظاهر شود.
این شکاف بین مسیر واقعی و طراحی فعلی، مسئله‌ی باز شماره‌ی یک است.

مخاطبان دیگر (کسی که کافه را کشف می‌کند، مشتری قدیمی) تأیید نشده‌اند و نباید
مبنای تصمیم قرار بگیرند.

## Product Purpose

منوی دیجیتال و صفحه‌ی معرفی ال کافه در اصفهان. هدف: مشتری بتواند سر میز،
بدون منوی کاغذی، آیتم‌ها و قیمت‌ها را ببیند و انتخاب کند.

## Operating Context

- ورود از طریق QR سر میز، روی موبایل شخصی.
- سایت کاملاً استاتیک است و روی GitHub Pages سرو می‌شود.
- زبان فارسی، جهت راست‌به‌چپ.

## Capabilities and Constraints

**دامنه و میزبانی — تأییدشده**

- دامنه: **`lcafe-esf.ir`**. همه‌ی آدرس‌های مطلق در کد (canonical، تگ‌های
  پیش‌نمایش، و معرفی ساخت‌یافته) روی همین تنظیم شده‌اند.
- میزبان: **پارس‌پک، هاست لینوکس اشتراکی، لوکیشن ایران**. لوکیشن داخلی انتخاب
  آگاهانه است چون مخاطب داخل کافه روی دیتای همراه است.
- میزبانی روی GitHub Pages موقتی بود و جایگزین می‌شود.
- هاست باید **PHP** داشته باشد؛ سایت دیگر فایل ثابت محض نیست.

**پنل مدیریت و منبع محتوا — تصمیم تأییدشده**

صاحب محصول پنل مدیریت می‌خواهد تا مشخصات آیتم‌ها را عوض کند و آیتم اضافه یا
کم کند. برای جلوگیری از تضاد دو نویسنده روی یک فایل، این تفکیک انتخاب شد:

- **طراحی** در `.dc.html` می‌ماند و ابزار design-canvas همچنان مالکش است.
- **محتوای منو** به `menu.json` منتقل می‌شود و پنل مدیریت فقط همان را می‌نویسد.
- پایگاه‌داده استفاده **نمی‌شود**: ۸۰ ردیف که یک نفر ویرایش می‌کند، به MySQL
  نیاز ندارد، و یک فایل، پشتیبان‌گیری و سطح حمله را ساده نگه می‌دارد.

**رفتار مشتری در سایت — تصمیم تأییدشده**

- مشتری فقط **می‌بیند**. سفارش، سبد خرید، و انتخاب آیتم در سایت وجود ندارد و
  ساخته نمی‌شود.
- تراشه‌های گزینه (۶۰/۴۰، ۱۰۰) اطلاعاتی‌اند و هرگز تعاملی نمی‌شوند.
- در نتیجه «ردیابی گزینه‌های انتخابی» موضوعیت ندارد؛ فقط **آمار بازدید** لازم
  است، ترجیحاً آمارگیر خودمیزبان روی همان هاست تا داده داخل کشور بماند.

**ساختار فایل‌ها**

- منبع حقیقت: `Landing Hero Background.dc.html` و `Menu.dc.html`.
- `index.html` و `menu.html` خروجی تولیدشده‌ی `build.py` هستند و **هرگز نباید
  دستی ویرایش شوند**.
- `support.js` و `image-slot.js` را ابزار design-canvas تولید می‌کند؛ دست‌نزدنی.
- بعد از هر تغییر: `py build.py`
- بعد از افزودن عکس: `py optimize_images.py`

**قیمت‌ها**

- قیمت به صورت **عدد خالی با ارقام فارسی و بدون هیچ واحدی** نمایش داده می‌شود.
  مثال: `۶۱۰`.
- این تصمیم آگاهانه‌ی صاحب محصول است. هیچ کار بعدی حق ندارد «تومان»،
  «هزار تومان»، جداکننده‌ی هزارگان یا زیرنویس واحد اضافه کند.
- در نشانه‌گذاری فعلی قیمت داخل `<strong>` (و در بخش افزودنی‌ها داخل `<b>`)
  قرار دارد.

**محتوای منو**

- ۱۴ دسته، ۸۰ آیتم، ۹۰ اسلات تصویر.
- ۶۷ قیمت واقعی وارد شده است.
- **۱۳ قیمت هنوز جای‌نگهدار `[قیمت]` هستند و پر نشده‌اند.** این‌ها باید از
  صاحب محصول گرفته شوند؛ هیچ عددی نباید حدس زده شود.

## Brand Commitments

- نام: **ال کافه** / L Cafe
- ارزش‌های اعلام‌شده در متن صفحه: احترام، سلامت، کیفیت.
- دارایی‌های بصری موجود و تثبیت‌شده:
  - لوگو: `uploads/L_Cafe_Full_NoTagline_White.svg`
  - پترن «ال»: `assets/l-cafe-pattern-inverted-tight.svg`
  - رنگ‌ها: زرشکی `#471019` و کرم `#F3F1EC`
  - فونت‌ها: Vazirmatn (متن) و Sahel Bold (تیتر) — هر دو self-host با
    مجوز OFL. Sahel فقط وزن ۷۰۰ دارد؛ هرگز وزن دیگری از آن خواسته نشود.

## Evidence on Hand

**موجود در ریپو**

- نشانی: خیابان چهارباغ بالا، نبش کوچه یحیی خان، مجتمع متروپل
- تلفن: ۰۹۱۳ ۰۰۰ ۵۷۶۸
- اینستاگرام: `@lcafe.esf`
- عکس فضای کافه: `assets/cafe-interior.webp`
- ۶۰ عکس بهینه‌شده‌ی منو در `assets/menu/opt/`

**تأییدشده ولی هنوز در سایت نیامده**

- **ساعت کاری: ۷ تا ۲۳.** موجود و قابل استفاده.
- **موقعیت روی نقشه:** موجود است، ولی لینک یا مختصات هنوز داده نشده.
- **عکس‌های بیشتر از فضا:** موجود هستند، اما صاحب محصول صریحاً گفته موجود
  بودن به‌تنهایی دلیل استفاده نیست. افزودنشان نیاز به تصمیم جداگانه دارد.

**غایب — نباید ساخته شود**

- هیچ نظر مشتری، امتیاز، جایزه، تعداد شعبه یا ادعای کیفیت تأییدنشده‌ای وجود
  ندارد. هیچ‌کدام نباید اختراع شوند.
- منوی ال کافه سفارش آنلاین، رزرو یا پرداخت ندارد.

## Product Principles

1. **مسیر کوتاه باشد.** هر تصمیمی اول با معیار «مشتری سر میز که QR را اسکن
   کرده و منو می‌خواهد» سنجیده می‌شود، بعد با هر چیز دیگر. لندینگ باید او را
   بدرقه کند، نه معطل.
2. **عدد خالی بماند.** سادگی نمایش قیمت یک انتخاب است، نه یک نقص.
3. **هیچ محتوایی اختراع نمی‌شود.** جای خالی به‌صورت جای خالی باقی می‌ماند تا
   صاحب محصول پرش کند.
4. **منبع را ویرایش کن، نه خروجی را.** هر تغییری روی `.dc.html` و سپس build.
5. **متن هرگز نامرئی نمی‌شود.** الگوی موجود در کد — که انیمیشن فقط بعد از
   آماده بودن جاوااسکریپت مسلح می‌شود و چند مسیر پشتیبان دارد — یک تعهد است،
   نه یک جزئیات پیاده‌سازی.

## Accessibility & Inclusion

- فارسی و RTL کامل.
- سایت روی موبایل و اغلب با یک دست استفاده می‌شود.
- تعهد موجود در کد که باید حفظ شود: پشتیبانی از `prefers-reduced-motion`،
  لینک پرش به محتوا، و حالت فوکوس نمایان.
- شکاف شناخته‌شده: از ۹۰ اسلات تصویر فقط ۲ تا متن جایگزین دارند.
